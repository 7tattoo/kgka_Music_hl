# 二进制资源直接入库（不再走 git-lfs）。
# 原因：CI（GitHub Actions）checkout 时若未拉取 LFS 对象，
# 工作区里的 *.png 会是 LFS 指针文本，导致 aapt2 编译失败：
#   ERROR: .../ic_launcher_monochrome.png: AAPT: error: file failed to compile.
# 这些图标/图片体积很小（合计约 2MB），直接作为普通 Git 对象存储更稳妥。
*.png binary
*.jpg binary
*.ico binary
*.jar binary
*.jks binary
*.pbxproj -text
*.metadata -text
